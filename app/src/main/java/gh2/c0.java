package gh2;

import android.os.HandlerThread;

/**
 * Compatibility type for HandlerThread allocations rewritten by the official client build.
 */
public class c0 extends HandlerThread {
    public c0(String name) {
        super(name);
    }
}
